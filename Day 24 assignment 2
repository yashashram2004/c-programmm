#include <stdio.h>
#include <math.h>

int main(){

	int total,rem,num,temp,i;
	
	
	scanf("%d", &num);
	temp = num;
	
	for(i = 0;i<2;i++){
		while(temp != 0){
			rem = temp % 10;
			total += rem;
			temp /= 10;
		}
	
		temp = total;
		if(total > 10){
			total = 0;
		}
	}
	
	printf("%d", total);	
}
